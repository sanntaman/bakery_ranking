<h1>全投稿一覧</h1>
<ul>
  <% @reviews.each do |review| %>
    <li>
      <%= review.comment %>
      <%= link_to '削除', admin_review_path(review), method: :delete, data: { confirm: '本当に削除しますか？' } %> 
    </li>
  <% end %>
</ul>